#!/bin/bash
#PBS -N fsdiloco_p1_match
#PBS -q debug-g
#PBS -W group_list=xg24i002
#PBS -l select=1:mpiprocs=1
#PBS -l walltime=00:01:00
#PBS -j oe

set -eEuo pipefail
trap 'echo "[ERROR] Phase 1 matched performance failed at line $LINENO" >&2' ERR

: "${FS_DILOCO_SHARED_ROOT:?FS_DILOCO_SHARED_ROOT is required}"
PROJECT_ROOT="${PROJECT_ROOT:-${PBS_O_WORKDIR:-$PWD}}"
PYTHON_BIN="${PYTHON_BIN:-$PROJECT_ROOT/.venv/bin/python}"
STAMP="${STAMP:-$(date +%Y%m%d-%H%M%S)}"
OUTPUT="${OUTPUT:-$PROJECT_ROOT/reports/DOING/fsb_decoupled_diloco_plan_02/artifacts/${STAMP}_phase1-matched-performance_pass.json}"
DESCRIPTOR="$FS_DILOCO_SHARED_ROOT/control/run_descriptor.json"

cd "$PROJECT_ROOT"
readarray -t descriptor_source < <(
  "$PYTHON_BIN" - "$DESCRIPTOR" <<'PY'
import json
import sys

with open(sys.argv[1], encoding="utf-8") as handle:
    descriptor = json.load(handle)
print(descriptor["git_commit"])
print(descriptor["source_fingerprint"])
print(int(bool(descriptor["git_dirty"])))
PY
)
readarray -t actual_source < <(
  "$PYTHON_BIN" - "$PROJECT_ROOT" <<'PY'
import sys

sys.path.insert(0, sys.argv[1] + "/scripts/miyabi")
from capture_source_identity import capture

identity = capture(__import__("pathlib").Path(sys.argv[1]))
print(identity["git_commit"])
print(identity["source_fingerprint"])
print(int(identity["git_dirty"]))
PY
)
test "${actual_source[0]}" = "${descriptor_source[0]}"
test "${actual_source[1]}" = "${descriptor_source[1]}"
test "${actual_source[2]}" = "${descriptor_source[2]}"

"$PYTHON_BIN" -m fs_diloco.tools.phase1_matched_performance \
  --run-root "$FS_DILOCO_SHARED_ROOT" \
  --output "$OUTPUT"
printf 'PLAN02_PHASE1_MATCHED_PERFORMANCE_ARTIFACT=%s\n' "$OUTPUT"
