# plan04 PREFORMAL current-state 复审

## 结论

- Verdict：`APPROVE`
- 新 `FINAL_COMMON_TARGET`：`7145197f3209fa67727bb0d458d0db38a81eb86d`
- Source fingerprint：`sha256:fcecd04f3ca5720cc3cd576889d8c20ad5d3f9dd9dd5725c7415b0549cdffa35`
- 13 个 formal source scope 均 clean；没有 unfinished PBS job。

## 复审范围

以 `d0acc5f9f95cb3d3885baf947319b93d84caeff1` 的完整 current-state 审查为基础，重新核对该 target 后唯一 source 变更：`tools/summarize_runs.py` 的 30% comparison 判定及对应 tests。此前对 launcher、authority、fault oracle、source identity、PBS、cleanup 和 obsolete surface 的结论不变。

## 正式 normal 发现及处理

- 旧 target 的 normal 产品运行完整完成：version 25、100 个 applied 200-step cycle、八 learner 与一个 syncer attestation、全部 actor FINISH、live syncer 只保留最新 weight；loss 相对两个 baseline 分别只升高 `8.93%` 和 `6.63%`。
- 唯一失败是旧 checker 对更短 training time 使用绝对差：normal `604.33s`，DDP `1129.11s`，periodic `1058.37s`。当前 quorum-4 workload 只需要 20,000 个 applied contributor optimizer steps，而 baseline 共执行 40,000 个 rank optimizer steps，因此缩短约一半符合当前设计。
- 当前 checker 使用 signed regression：identity mismatch 或 loss/time 升高超过 30% 才要求调查；指标降低不再作为回退。没有降低 loss 或变慢的通过门槛，也没有改变任何 runtime 产品 oracle。
- Mutation 覆盖两面：指标升高超过 30% 仍失败；指标降低超过 30% 不失败。

## 验证

- Miyabi compute `2540684.opbs`：Ruff format/check 通过；focused tests `25 passed`。
- Clean target 完整 pytest：`595 passed in 35.81s`。
- Source capture 确认 commit、fingerprint 与 clean scopes；`qstat` 无 unfinished job。

旧 target 的 baseline/normal artifact 因 source scope 变化仅保留为 failure/root-cause evidence，不参与最终结论。正式实验必须全部重跑并绑定本报告的新 target。
