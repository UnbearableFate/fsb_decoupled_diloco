# plan04 PREFORMAL current-state review

## Target

- Commit: `d2117d0c83f205eabd2246feec82a77c2e0230c0`
- Source fingerprint: `sha256:2c651ee95fda24539fa597b85753706f16d40b847cde4cbeedba208f01a7998d`
- Source scopes: `fs_diloco`, `configs`, `do_experiments`, `scripts/miyabi`, `tests`, `tools`, `torch_ddp_baselines`, `pyproject.toml`, `README.md`, `docs`, `plans/00-RESEARCH_PLAN.md`, `website/app`, `website/scripts`

## Review result

**APPROVE**

The only change since the previous approved target is the user-directed scheduler policy: plan04 supervisor, actors, scheduler-authorized replacement learners, and standalone baselines now use `debug-g` with a 30-minute wall-time. The launcher, checked-in PBS directives, fault config, tests, README, and plan state one consistent policy. No training workload, protocol state transition, authority schema, oracle, summary computation, or metric threshold changed.

`qstat --rscuse` showed lower utilization in `debug-g/interact-g` than `regular-g`. The 30-minute budget retains substantial margin over the observed approximately 10–19 minute completed workloads. PBS syntax and literal group directives pass static validation. Miyabi compute validation on `2541387.opbs` passed Ruff check, 55 focused tests, the full suite (`595 passed in 35.97s`), and Ruff format check.

No blocking finding remains. The scheduler-policy commit is functionally equivalent to the previously approved `7145197f3209fa67727bb0d458d0db38a81eb86d` target: its diff changes only queue/wall-time declarations and the tests/documentation that freeze them. Per the user's evidence policy, completed artifacts on that prior functional target remain valid; experiments are rerun only after functional-logic changes. The final manifest must record this exact scheduler-only equivalence instead of pretending the fingerprints are identical.
