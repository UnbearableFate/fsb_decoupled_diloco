# plan03-1 current-review remediation — review

## Identity and scope

- Reviewer: `minimax-m3` (`opencode-go/minimax-m3`).
- Review lane: cross-module integration, experiment validity, artifact/Checker evidence, and documentation drift.
- Plan: `plan03-1`. Review kind: `critical-incremental`. Review ID: `critical-current-fs-diloco`.
- Continuity base: `5b474d5c1735beb8cca922fd6cc7b6304926df2c`.
- Frozen target under review: `74ecd4fb64311c69ae0d758d8c1d99b27a9c5572`.
- Tracked disposition: `reports/DOING/code_review/plan03-1/critical-current-fs-diloco/finding-dispositions_4ebee6339fb76f63127874c655d7b109b2ec0b39.md`.
- Runner metadata: `.review-packet/{diff-stat,changed-files}.txt`, `target.diff` (excluded from findings; not a product change).
- Action: read-only. No files edited, no Git state touched, no jobs submitted, no test suites executed, no external state written.

## Inspected scope

- Product: `fs_diloco/{core,protocol,storage,runtime,services,tools,modeling,observability}/...`, `fs_diloco/learner.py`, `fs_diloco/syncer.py` (now thin module wrappers).
- Configuration: `configs/full_protocol_{static,functional,dynamic}.yaml`.
- Launchers / Checkers / validation suite: `scripts/miyabi/run_full_protocol.pbs`, `run_full_protocol_allocation.sh`, `run_full_protocol_rank.sh`, `run_multi_agent_review.pbs`, `check_full_protocol_run.py`, `check_plan_completion.py`, `run_validation_suite.py`.
- Tests: `tests/runtime/test_syncer_{composition,startup_admission,fault_boundary}.py`, `tests/harness/test_full_protocol_harness.py`, `tests/harness/test_validation_suite.py`, `tests/harness/test_plan_completion.py`, `tests/architecture/test_plan_complete_dead_surfaces.py`, `tests/test_config.py`, `tests/storage/test_{schema,authority_operational,dynamic_admission_request,contributor_progress}.py`, `tests/tools/test_resolve_scheduler_uncertainty.py`, and surveys of all other modules under `tests/`.
- Documentation / artifacts: `README.md`, `docs/{configuration,design,operations}.md`, `pyproject.toml`, `.python-version`, `reports/DOING/plan03-1/{identity-authority,artifact-schema,requirement-matrix,formal-ladder-manifest}.{md,csv,json}`, `reports/DOING/plan03-1/code_review.md`, `reports/DOING/plan03-1/workflow_state.yaml`, the Codex-2 review schema in `artifact-schema.md`.
- Invariants exercised: leader lease fencing and stale-token rejection, command-replay idempotency, contributor-fence currency on admission, ingest, and terminal ack, static replacement authorization, schema-bound `ContributorResumeState.last_update_id` plumbing, gate-artifact self-proof and JUnit count validation, structured validation evidence, dead-surface content scan, takeover-boundary single source, fault-scenario binding generation/history pinning.

## Findings vs. the tracked disposition

### FSD-H1 (Critical) — validation gate false-PASS resistance — CLOSED
- Evidence: `scripts/miyabi/run_validation_suite.py:24-44` declares `FOCUSED_TESTS` and `REQUIREMENTS = ("UNIT-01", "HARNESS-01")`; `:166-192` parses per-step JUnit XML and projects `tests/failures/errors/skipped`; `:233-294` `_validate_artifact` rejects a PASS when any pytest step has `tests < 1` or any failure/error/skipped > 0; `:391-403` also fails the run when the JUnit machine result is not exact; `:274-295` the structured artifact requires both `metrics.steps[i].{tests,failures,errors,skipped}` and `evidence_paths` to enumerate the JUnit XML files.
- Test: `tests/harness/test_validation_suite.py:147` records machine-checkable pytest counts; `:181` rejects zero-collected and skipped pytest with exit zero.
- Invariant satisfied: the artifact is the success oracle, not the return code.

### FSD-H2 (High) — unregistered learner replacement cannot pass the normal gate — CLOSED
- Evidence: `scripts/miyabi/check_full_protocol_run.py:629-647` unconditionally pins the expected binding generation to `{learner: 2 if learner == replacement_learner else 1}` and exact binding history; `:714` distinguishes the registered fault scenario from `none`; `tests/harness/test_full_protocol_harness.py:523,606-623` adds `unregistered_replacement` mutation to the aggregate mutation matrix and expects FAIL.
- Invariant satisfied: a fault run can no longer be certified as a normal scenario.

### FSD-H3 (High) — resumed contributor can produce a valid graceful terminal ack — CLOSED
- Evidence: `fs_diloco/protocol/data_cursor.py:71-93` `ContributorResumeState` carries `last_update_id`; `fs_diloco/protocol/authority.py:216-279` `DynamicAdmission` mirrors the field; `fs_diloco/storage/authority.py:1377-1384,2992,5474,6007` plumbs `last_update_id` through receipt ingest, dynamic admission response, terminal contributor fence projection and decoding; `fs_diloco/runtime/syncer.py:577,791,801,814` carries the value across replay/resume/ingest boundaries; `fs_diloco/runtime/learner.py:479` initialises `last_cycle_update_id = admission.resume.last_update_id`.
- Behavioral test: `tests/storage/test_authority_operational.py:466-518` admits a dynamic replacement after a `proposal_expected=True` receipt, asserts `second.resume.last_update_id == receipt.planned_update_id`, and proves that `acknowledge_terminal_contributor(..., final_update_id=second.resume.last_update_id)` succeeds; `tests/storage/test_dynamic_admission_request.py:55-79` asserts the response projection; `tests/storage/test_contributor_progress.py:25-69` asserts the durable progress row stores the value; `tests/storage/test_schema.py:69` asserts the column exists.
- Invariant satisfied: replaced or resumed contributors no longer force a `hard_crash` due to missing `final_update_id`.

### FSD-H4 (High) — Checker fault-gate logic has behavioural coverage — CLOSED
- Evidence: `tests/harness/test_full_protocol_harness.py:414-447` parametrizes positive acceptance over `("learner_replacement", 2, ["released"], 2, 1)` and `("syncer_takeover", 3, ["expired", "released"], 1, 2)` and asserts `status == "PASS"`; `:475-505` parametrizes negative mutations over `replacement_history`, `replacement_evidence`, `takeover_boundary`, `takeover_linkage` and asserts `status == "FAIL"`; the aggregate mutation matrix at `:508-637` adds `unregistered_replacement` to the normal-scenario fixture.
- Invariant satisfied: every learner-replacement and syncer-takeover branch in `check_full_protocol_run.py:643-663,778-810,812-830,838-855` is now executed against a fixture with a paired mutation.

### FSD-M1 (Medium) — syncer composition has behavioural coverage — CLOSED
- Evidence: `tests/runtime/test_syncer_composition.py:47-92` exercises `_admit_requests` against a real `LeaderAuthority` with a static request followed by a replacement attempt that lacks a matching authorization, asserts `disposition["outcome"] == "rejected"`, `error_type == "MembershipFenceError"`, `binding_generation == 1`; `:95-129` and `:132-152` exercise `_ingest_proposals` with a leader that raises `CommandConflictError` for both receipt and proposal ingestion and asserts the exception propagates.
- Invariant satisfied: the static-replacement authorization decision and the receipt/proposal ingest paths have a behavioural owner.

### FSD-M2 (Medium) — fault text overclaim resolved honestly — CLOSED (option (a))
- Evidence: `reports/DOING/plan03-1/identity-authority.md:31-35` (F-LEARNER-REPLACE) and `:49-54` (F-SYNCER-TAKEOVER) explicitly state that *stale-fence / stale-token rejection is proven independently by authority unit tests; the sequential process injection does not claim to execute a live old writer / concurrent stale writer.* No SIGSTOP/SIGCONT injection was added to `run_full_protocol_rank.sh`; no durable rejection artifact is required from the multi-node oracle.
- Invariant satisfied: the registered fault oracle no longer claims evidence the harness structurally cannot produce.

### FSD-M3 (Medium) — repository config discovery is CWD-independent and non-vacuous — CLOSED
- Evidence: `tests/test_config.py:20-22` resolves `ROOT = Path(__file__).resolve().parents[1]` and asserts `CONFIGS, "no repository configs discovered"`; `tests/tools/test_resolve_scheduler_uncertainty.py:42-46` was already CWD-independent via `parents[2]`.
- Invariant satisfied: future CWD-changed pytest runs will fail loudly on empty config discovery.

### FSD-M4 (Medium) — content scan with explicit wire-type allowlist — CLOSED
- Evidence: `tests/architecture/test_plan_complete_dead_surfaces.py:104-151` enumerates tracked text files, runs a content regex that excludes the wire-format exemptions, asserts the violation set is empty, and asserts the only AST-named versioned wire types are `CycleReceiptV1` and `FullUpdateProposalV2`; `:99` asserts the path-only scan is non-vacuous.
- Invariant satisfied: `CycleReceiptV1` and `FullUpdateProposalV2` are correctly treated as versioned wire artifacts (data-contract versions live in `fs_diloco/core/versions.py`), not as product generation suffixes. The reviewer disposition's "partially accepted-open" position is honoured: the V1/V2 type-suffix question is decided (retained, exempt), and the scan is now content-based and non-vacuous.

### FSD-M5 (Medium) — narrow ingestion exception handling — CLOSED
- Evidence: `fs_diloco/runtime/syncer.py:839-858` (receipt) and `:861-881` (proposal) re-raise `AuthoritySchemaError`, `CommandConflictError`, `StaleLeaderTokenError` and downgrade only `(MembershipFenceError, OSError, ValueError)` and `(MembershipFenceError, ProposalPayloadError, OSError, ValueError)` respectively to telemetry.
- Invariant satisfied: integrity failures can no longer be swallowed into unbounded telemetry-only retries.

### FSD-M6 (Medium) — single-source takeover boundary — CLOSED
- Evidence: `scripts/miyabi/run_full_protocol.pbs:28` defines `readonly SYNCER_TAKEOVER_BOUNDARY_VERSION=2`, exports it on `:40`, and feeds `--syncer-takeover-boundary-version "$SYNCER_TAKEOVER_BOUNDARY_VERSION"` to the checker on `:57`; `scripts/miyabi/run_full_protocol_allocation.sh:12,105` declares and forwards the same variable into the rank script environment; `scripts/miyabi/run_full_protocol_rank.sh:177` consumes `FS_DILOCO_FAULT_PAUSE_AFTER_COMMITTED_VERSION="$SYNCER_TAKEOVER_BOUNDARY_VERSION"`; `scripts/miyabi/check_full_protocol_run.py:389,850,1221` threads `syncer_takeover_boundary_version` into `validate_run` and uses it in the `boundary.get("committed_version", -1) != syncer_takeover_boundary_version` assertion.
- Invariant satisfied: launcher and checker consume the same configured value.

### FSD-M7 (Medium) — one current invocation surface — CLOSED
- Evidence: `fs_diloco/cli.py` deleted; `tests/test_cli.py` deleted; `[project.scripts]` block removed from `pyproject.toml:25-37`; `fs_diloco/protocol/__init__.py` reduced to one docstring statement; `fs_diloco/learner.py` and `fs_diloco/syncer.py` are thin wrappers around `runtime.learner_entrypoint.main` and `runtime.syncer_entrypoint.main`; the only consumers in launchers (`scripts/miyabi/run_full_protocol_rank.sh:26,162`, `scripts/miyabi/run_learner.pbs:44`, `scripts/miyabi/run_syncer.pbs:21`) and `docs/operations.md` use `python -m fs_diloco.{learner,syncer}` or `python -m fs_diloco.tools.*`.
- Test: `tests/architecture/test_plan_complete_dead_surfaces.py:154-162` enforces the invariant (`scripts` key absent from `pyproject.toml`, no `fs_diloco/cli.py`, single-statement `protocol/__init__.py`).

### FSD-L1 (Low) — duplicated owner-path derivation — CLOSED
- Evidence: `scripts/miyabi/check_full_protocol_run.py:598-602` calls `RunPaths(run_root).epoch_stop_path(int(terminal_row["finalized_by_epoch"]), str(terminal_row["finalized_by_owner_id"]), int(terminal_row["generation"]))` instead of the inline `hashlib.sha256(owner_id.encode("utf-8")).hexdigest()[:12]` derivation.
- Invariant satisfied: the checker's epoch-stop path always agrees with `RunPaths.owner_short`.

### FSD-L2 (Low) — `syncer_epochs.final_state` domain — CLOSED
- Evidence: `fs_diloco/storage/schema.sql:52` constrains `final_state IN ('released', 'expired', 'error')`; `tests/storage/test_schema.py:70-71` asserts `'terminal'` is not in the constraint and that `'released'`, `'expired'`, `'error'` are present; no writer in `fs_diloco/storage/authority.py` (`:905, :1012, :1038`) produces `'terminal'`.
- Invariant satisfied: schema revision 11 exactly matches the writable domain.

### FSD-L3 (Low) — Python version triple declaration — CLOSED
- Evidence: `pyproject.toml:10` `requires-python = ">=3.13"`; `:29` `target-version = "py313"`; `.python-version` = `3.13`.
- Test: `tests/architecture/test_plan_complete_dead_surfaces.py:156-158` enforces all three.

### FSD-L4 (Low) — stale default profile names — CLOSED
- Evidence: `fs_diloco/core/config.py:98` `RunSection.name = "full_protocol"`; `:108` `ModelSection.name_or_path = "synthetic-tiny"`; `:120` `DataSection.dataset_name = "synthetic"`. The resolved configs use the same names; defaults no longer reference a workload that does not exist.

### FSD-L5 (Low) — swallowed `release_leader` failure — PARTIALLY CLOSED
- Evidence: `fs_diloco/runtime/syncer_entrypoint.py:257-264` removes the `try/except Exception: pass` swallow; the new layout calls `authority.release_leader(token)` and propagates any exception through the outer `try/finally` (which still ensures `authority.close()`). `tests/runtime/test_syncer_startup_admission.py:137-232` proves a successful candidate does not swallow `StaleLeaderTokenError`.
- Residual: the richer FSD-L5 remediation — distinguishing "a successor already fenced me" (safe to ignore) from "my own release failed" (must be surfaced) — is **not** implemented. The current code propagates both branches identically, with no separate logging or artifact projection for the own-failed case. This is consistent with the disposition's stated minimum ("distinguish … from …" was the recommendation, not the strict requirement), but the resulting exit code conflates the two cases and the durable artifact does not record the distinction. Severity: Low; does not invalidate the original L5 counterexample (correct run failing the checker), which is fixed by the un-swallowed propagation; but does not fully realise the recommended split.

### FSD-L6 (Low) — single contributor-count input in static config — CLOSED
- Evidence: `configs/full_protocol_static.yaml:25-30` and `configs/full_protocol_functional.yaml:25-30` declare only `membership.mode: static` and timing fields, not `stream_pool_size`, `bootstrap_instances`, or any `scaling.*` block; `fs_diloco/core/config.py:352-365` `_validate_source_shape` rejects static configs that restate `stream_pool_size` / `bootstrap_instances` or carry inert `scaling.*`; `:535-537` derives both values from `sync.num_learners`; `:568-644` only validates the `scaling.*` cross-field constraints when `scaling.enabled`; `fs_diloco/core/config.py:343-349` `config_to_dict` strips `stream_pool_size` / `bootstrap_instances` / `scaling` from static payloads; `docs/configuration.md:32-37` documents the invariant.
- Test: `tests/test_config.py:33-46` parametrizes over both static configs and asserts the resolved `stream_pool_size` / `bootstrap_instances` equal `sync.num_learners` and that `scaling` is absent; `:49-64` rejects three forms of repeated or inert static capacity fields.

### FSD-L7 (Low / informational) — dynamic membership evidence boundary — DOCUMENTED
- Evidence: `reports/DOING/plan03-1/identity-authority.md:61-64` explicitly states: *Dynamic membership remains part of the single current Full Protocol and has focused capacity, scheduler, admission, fencing, replay and terminal unit coverage. It is not a registered plan03-1 multi-node fault scenario: the `FAULT-4L1S-01` acceptance topology is the two static scenarios above.* This records the unit-evidence boundary for `FINAL-01`'s "100% bound to same-target evidence" claim.

## Priority invariant audit

| Invariant | Status |
|---|---|
| Static replacement admission is authorized and fenced by the real authority | satisfied (`_validate_source_shape` + `_validate_and_replace_static_attempt` + `test_active_static_attempt_replacement_requires_matching_authorization` + `_test_syncer_composition`) |
| Schema, command-conflict, stale-leader integrity failures cannot be swallowed by receipt/proposal scanning | satisfied (`syncer.py:850-851,873-874` re-raise `AuthoritySchemaError`, `CommandConflictError`, `StaleLeaderTokenError`) |
| Takeover boundary has one owner and the multi-node oracle does not claim a live stale writer | satisfied (single `SYNCER_TAKEOVER_BOUNDARY_VERSION` source; `identity-authority.md:33-35,52-54` explicitly demote the stale-writer claim to unit-only evidence) |
| Static config has one contributor-count input and no inert scaling surface; dynamic remains strict when enabled | satisfied (`_validate_source_shape`, `_normalize_and_validate` only enforces scaling cross-field when `scaling.enabled`, and `scaling.enabled` requires `membership.mode == "dynamic"`) |
| Schema revision 11 exactly matches the writable `syncer_epochs.final_state` domain | satisfied (`fs_diloco/storage/schema.sql:52`, `tests/storage/test_schema.py:70-71`) |
| One current invocation surface; no compatibility facade | satisfied (single `python -m fs_diloco.{learner,syncer,tools.*}`; `cli.py`, `[project.scripts]`, `protocol/__init__.py` 32-symbol facade all removed) |
| Repository discovery/content tests cannot vacuously pass | satisfied (`test_config.py:22` `assert CONFIGS, …`; `test_plan_complete_dead_surfaces.py:99,123,130` non-empty tracked/text assertions; `test_no_product_generation_suffix_in_tracked_contents` content scan) |
| Schema-2 Codex review artifacts and four current external reviewer lanes accurately represent best-effort review without treating absence as approval | satisfied (`artifact-schema.md:84-90` declares `review_artifact_version: 2` and explicit "reviewer outage is not approval"; `plans/workflow.md:43-44,113-114,351-352` and `scripts/miyabi/run_multi_agent_review.pbs:67,496-527` retarget the fourth lane to `opencode-go/minimax-m3` / `OPENCODE_MINIMAX_MODEL`, deleting the obsolete Kimi alias; the disposition file explicitly logs `GLM-5.2, DeepSeek V4 Flash, and MiniMax M3 timed out and produced no valid report` and the verdict is bound to the one valid Claude report) |

## Observations (non-blocking, non-product)

- `fs_diloco/core/versions.py` and the architecture test together encode the V1/V2 wire-type exemption as an explicit AST equality assertion (`{"CycleReceiptV1", "FullUpdateProposalV2"}`); this is the right call because `CYCLE_RECEIPT_FORMAT_VERSION = 1` and `PROPOSAL_FORMAT_VERSION = 2` already live as separate wire-data-contract constants in the same file. Challenging this decision would require showing a current conflict, of which there is none.
- `reports/DOING/plan03-1/workflow_state.yaml:65-78` still lists all `FSD-M1-M7` and `FSD-L1-L6` IDs under `open_blocking_findings` with `target_commit: null`. This pre-freeze state is consistent with the workflow (allowed_next_action: `freeze_current_review_remediation_target`); the user is expected to transition the workflow after this review disposition.
- FSD-L5's richer split (successor-fenced vs own-release-failed) is the only residual gap between the disposition's "minimum fix" and the recommended fix. Severity: Low. Recommendation: either widen `test_successful_candidate_does_not_swallow_release_failure` to assert a distinct log/artifact line for `StaleLeaderTokenError` vs other exception types, or leave it as a follow-up.

## Conclusion

Every accepted FSD-M1 through M7 and FSD-L1 through L6 item is closed in the current target `74ecd4fb64311c69ae0d758d8c1d99b27a9c5572`, with the sole residual being the FSD-L5 successor-vs-own-release distinction (Low severity; minimum fix applied; richer recommendation not implemented). No H1-H4 finding is regressed. All priority invariants hold. The four-lane external reviewer lane names and the schema-2 Codex review artifact accurately represent best-effort availability without treating reviewer outage as approval.

Verdict: APPROVE
