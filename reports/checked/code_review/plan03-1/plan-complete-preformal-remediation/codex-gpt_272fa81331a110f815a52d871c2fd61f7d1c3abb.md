# Codex preformal remediation critical-incremental review

- Plan: `plan03-1`
- Review kind: `critical-incremental`
- Rejected base: `1f02e7b7a4d96cbacca7451b8b902ceebb34de2e`
- Reviewed target: `272fa81331a110f815a52d871c2fd61f7d1c3abb`
- Validated source commit: `2b0c9a004e04af0907ce7766d4d9df47b29cf545`
- Validated source fingerprint:
  `sha256:6b4615945eadb0eea33f344dab2c9d2ad8ce3fb5525bd4af4b5d530172b9af4f`
- External review: skipped by explicit user direction

## Scope inspected

I inspected the complete continuous remediation diff, both authority schema
bundles and schema identity/open checks, receipt-ingestion transaction,
contributor progress read and dynamic admission projection, static/dynamic
replacement and terminal lineage tests, completion manifest validation,
supporting-evidence path/hash ownership, validation artifact/JUnit construction,
the unrelated-support mutation, runtime retention wording, the retained raw
evidence, JUnit counts and PBS terminal metadata.

## Finding closure

### P4-R2-F1 — closed

`contributor_progress` now owns `last_update_id` directly. Receipt ingestion
writes the receipt and progress value in the same fenced SQLite command;
read-model and dynamic admission paths query only progress. The authority
schema is consistently revision 10 in the code constant, schema CHECK,
bootstrap metadata and tests. There is no migration, join fallback or second
representation. The focused test reads the physical progress column directly;
the existing static and dynamic replacement tests verify resume identity,
terminal acknowledgement and rejection of a stale live predecessor lineage.

### P4-R2-F2 — closed

The aggregate now requires registered supporting paths to be a subset of the
corresponding gate artifact's own `evidence_paths`. U1 requires set equality,
so its raw log and both distinct JUnit files are all tracked and hash-bound.
The new mutation substitutes an unrelated file for a runtime gate's registered
support and reaches the production aggregate rejection. Existing wrong-source,
self-input, untracked-input and non-internal-review mutations remain intact.

### P4-R2-F3 — closed

Every runtime gate and the aggregate retention policy now preserve the complete
run through completed checking and plan archive. Cleanup is authorized only
afterward through the exact gate PASS artifact, so completed mode cannot lose
the raw paths it revalidates.

## Validation evidence

PBS job `2520559.opbs` ran on the sole allocated compute host `mg0032` with the
registered one-node resources and exited 0. The affected group passed 54 tests.
The create-only producer passed Ruff format/lint, 169 focused tests and 527 full
tests with zero failures, errors or skips. The artifact, raw log and both JUnit
files are tracked and their recorded hashes match. The formal source scopes
were clean before and after execution.

No Critical, High, Medium or Low finding remains in the remediation or its
evidence. P4-R2-F1 through F3 are closed. The repository may proceed to the
over-baseline nine-node documentation-verification run; any subsequent source
or documentation change must still precede final common-target freeze.

Verdict: APPROVE
