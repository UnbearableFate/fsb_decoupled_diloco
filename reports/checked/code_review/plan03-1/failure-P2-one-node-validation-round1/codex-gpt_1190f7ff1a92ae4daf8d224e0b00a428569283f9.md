# Mandatory Codex logic-rewrite review

- Plan: `plan03-1`
- Review kind: `critical-incremental` after comprehensive failure review
- Failure target: `a701570a5762c05dd892b10599a27a793e6d1549`
- Rewritten target: `1190f7ff1a92ae4daf8d224e0b00a428569283f9`
- Finding: F1 exact-workload mutation rejected before Checker
- External review: skipped under the user's Codex-only review directive

## Review

The rewrite implements the comprehensive review without changing product code,
schema or the Checker acceptance requirement.

The direct SQLite statement now assigns processed and effective receipt tokens
to 15 in one statement. With local discarded tokens still 0 and retained tokens
still 16, the final row satisfies both current receipt constraints:
`processed = effective + local_discarded` and
`retained >= effective`. The context-managed connection commits on successful
exit. A readback inside the transaction must equal `(15, 15)`, proving the
mutation executed against the intended row before the Checker subprocess.

The positive fixture still contains the registered 16-token workload. Only the
`exact_workload` mutation receives the paired 15-token row. After the shared
aggregate assertions require exit 1, status FAIL, nonempty errors and cleanup
ineligibility, the test additionally requires the exact cycle-workload error.
This prevents an unrelated integrity, topology or terminal error from being
mistaken for sensitivity of the workload oracle.

The alternative constraint-disabling, cursor-only and second-config fixture
approaches rejected by the failure review were not introduced. The 10 other
mutations and all current product paths are unchanged.

## Findings

F1 is `fixed pending runtime verification`. No new Critical, High, Medium or
Low finding exists. The exact static/focused command may run as post-rewrite
attempt 4; success must then be followed by the complete one-node suite.

Verdict: APPROVE
