# Mandatory Codex P3 phase closure review

- Plan: `plan03-1`
- Review kind: `phase`
- Base commit: `9398e822ebe6cf9755e55567b18916802b93162f`
- Frozen target: `a133a98a431566dbd1aef1af6a7f496f2c301d38`
- Requirements: `FUNC-4L1S-01`, `FAULT-4L1S-01`
- External review: skipped under the user's Codex-only review directive

## Source and harness audit

The P3 target is a continuous descendant of P2 phase-final. I inspected the
complete increment in production authority code, Checker, harness tests,
module coverage, fault manifest, artifact contract, review/failure history and
tracked runtime evidence. The terminal total now comes from the all-history
token rollup rather than the prunable hot publication table; sparse zero-work
and archived positive-work states have separate regression assertions.

The aggregate Checker derives exact applied work from registered topology and
workload. It independently validates merged publication history, token rollup,
terminal SQLite authority, fixed controls and the immutable terminal control.
Its synchronized mutation changes all terminal projections together while
leaving publications/rollup correct, proving that the cross-authority oracle
rejects the previous false-PASS. Normal-run overshoot remains acceptable only
when every exact-workload receipt maps to a proposal and every non-applied
proposal is durably dropped with balanced token accounting.

The last one-node producer is a tracked, error-free PASS from clean source
commit `0e951518c3ef693b20a241e36fc8396d61f609a7`, fingerprint
`sha256:6b4964840a786276242608746f862ca3ce05ae48f256d8755eadb58cfd4454e5`.
It records Ruff format/lint success, 120 focused tests and all 506 repository
tests passing on PBS node `mg0004`. The later evidence commits do not change
any formal source scope.

## Five-node evidence audit

The final normal, learner-replacement and syncer-takeover artifacts are tracked
with their raw PBS logs. All three have exact source commit
`7528370e6b6635ecc7d6c2b40ac8e337c901826b`, the same fingerprint above,
`dirty=false`, status `PASS`, empty errors and exact five-node attestations.
Git confirms the formal source scopes are byte-identical between the executed
source and this frozen evidence target.

All three runs prove versions 0-4, four applied proposals per contributor,
5120 directly applied tokens, zero outstanding work, zero token balance,
acknowledged terminal fences, immutable publication hashes, released final
epoch and SQLite integrity. Manual cross-authority inspection confirms 5120 in
merged publications, rollup, terminal authority, fixed stop/summary and the
immutable stop even though online archival leaves only the final 1280-token
publication in the hot table.

- Normal job `2519926.opbs` completed in 15 seconds with 16 applied and 12
  adjudicated-dropped proposals.
- Learner-replacement job `2519935.opbs` completed in 17 seconds. The killed
  original exited 143; `learner_000` advanced binding generation 1 -> 2; old
  history is `replaced`; the exact successor contributed and acknowledged
  terminal state.
- Syncer-takeover job `2519945.opbs` completed in 29 seconds. The primary
  exited 137 after the registered version-2, outside-transaction,
  renewer-quiesced marker; epoch 1 expired and links to epoch 2; only the
  successor committed versions 3-4 and then released.

Scheduler history reports exit status zero for each exact bounded allocation.
Earlier normal false-negative and learner false-PASS artifacts remain tracked
and explicitly invalidated; they are not used as success evidence.

## Requirement conclusions

### FUNC-4L1S-01

The final normal artifact and raw authority evidence prove the complete 4+1
initialization, admission, training, receipt/proposal, selection, publication,
adoption, terminal and integrity path with exact durable workload accounting.

### FAULT-4L1S-01

The two final fault artifacts prove both registered process boundaries using
durable binding generations, history rows, epoch linkage, publication writers
and terminal authority. Process exits are used only as injection diagnostics.

## Findings

No Critical, High, Medium or Low finding remains. P3 may be marked complete.
This review does not promote the pending formal 8+1 or documentation gates.

Verdict: APPROVE
