# Codex preformal current-state review

- Plan: `plan03-1`
- Review kind: `preformal-current-state`
- Final common target: `1558e1112108ec38388cc2361b69e2cf78d49217`
- Source fingerprint:
  `sha256:9fdff5a7c613e044a7f97523ede2c566f60d4fc9fbf5a5cb4edd5c416ee06337`
- Requirements: `SURFACE-01`, `CONFIG-01`, `SCHEMA-01`, `CLEAN-01`,
  `ARCH-01`

## Review performed

I reviewed the complete tracked source target, not only its final documentation
increment. The retained runnable surface is the unversioned Full Protocol;
repository architecture tests and the module-owner inventory reject deleted
CLI, compatibility, versioned product and dead entrypoint surfaces. Config
loading has one strict current schema and the static/dynamic canonical files
use only the current keys. The authority initializer exposes only revision-11
`schema.sql` plus `schema_dynamic.sql`, with no migration or fallback path.

Ownership and dependency boundaries are explicit across configuration,
descriptor/source identity, SQLite authority, learner/syncer entrypoints,
proposal and receipt ingest, terminal service, Checker and cleanup. The
disposed FSD-R1 through FSD-R10 fixes remain present: exact behavioural
composition coverage, strict integrity failure propagation, SQLite-only
BUSY/LOCKED retry, required takeover-boundary identity, current config
discovery, effective AST dead-surface oracles and removal of obsolete
wrappers/defaults.

The final target's source scopes are clean in a detached worktree and reproduce
the frozen fingerprint. PBS `2521752.opbs` then passed Ruff format/lint, 186
focused tests and all 544 tests with non-empty JUnit projections and zero
failure, error or skip. Three exact-source five-node runs subsequently passed
normal, learner-replacement and syncer-takeover Checkers. These runtime results
are later ladder evidence; they do not substitute for this source review.

The restored external job `2521665.opbs` used four read-only snapshots with
unchanged digests. It produced no valid external report because of capacity,
timeout and invalid-output outcomes; this is recorded as unavailability, not
approval. Its incomplete traces exposed no concrete new finding after Codex
disposition.

No open Critical, High, Medium or Low finding remains for the five reviewed
requirements. The target is suitable for its remaining formal G1 and final
evidence review without any source-scope change.

Verdict: APPROVE
