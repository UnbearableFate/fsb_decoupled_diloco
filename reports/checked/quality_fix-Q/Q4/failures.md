# Q4 failures

## 2026-07-18 — PVE-01 evaluator contract RED (attempt 1)

- Command: `pytest -q tests/test_validation_eval.py` on Miyabi interactive job `2405414.opbs`, node `mg0006`.
- Expected RED: the dedicated validation evaluator contract is not implemented.
- Actual: collection failed with `ModuleNotFoundError: fs_diloco.tools.validation_eval`.
- Required behavior frozen by the test: explicit causal shift and predicted-token weighting; empty/non-finite fail-closed checks; latest-checkpoint identity with a deliberate non-latest escape hatch; atomic, idempotent, checkpoint-safe summary attachment.
- Evidence: `artifacts/20260718-0830_pve01-red-fail.log`.
- Next implementation: add the evaluator module and reuse the repository's exact text→tokens+EOS→non-overlap block pipeline; do not route the primary metric through lm-eval's task-specific WikiText adapter.

## 2026-07-18 — PVE focused command referenced a nonexistent test file (attempt 1)

- Command: `pytest -q tests/test_validation_eval.py tests/test_validation_submitter.py tests/test_data_shuffle.py tests/test_eval_lm_harness.py` on job `2405414.opbs`, node `mg0006`.
- Expected: evaluator and afterok submitter regression group runs.
- Actual: pytest exited before collection because `tests/test_eval_lm_harness.py` does not exist; no test executed.
- Confirmed cause: operator command error. The repository has no separate lm-harness test module; this does not identify an implementation defect.
- Evidence: `artifacts/20260718-0910_pve01-05-focused-green.log` (filename predates the result; content is authoritative).
- Next attempt: rerun the existing evaluator, submitter, and data-shuffle test files only, under a new artifact name.

## 2026-07-18 — PVE-01 legacy H full-validation failed (attempt 1)

- Command: dedicated evaluator on `codex_predict_gc_retry_wait0_fixed20_global50_full5000_20260717_073011`, CUDA BF16, batch 4, full configured validation split, with the explicit legacy source-identity waiver; Miyabi job `2405414.opbs`, node `mg0006`.
- Expected: a finite full-split validation result attached to H's terminal checkpoint.
- Actual: evaluation stopped before dataset/model work. Strict config loading rejected the historical resolved key `sync.upload_mode`, which current runtime deliberately removed.
- Confirmed cause: runtime config strictness is correct for new training, but the evaluator needs a narrowly scoped historical-snapshot loader that removes only keys listed in `REMOVED_CONFIG_KEYS`. Falling back to an unrelated current config would violate checkpoint/protocol identity and is not acceptable.
- Evidence: `artifacts/20260718-0920_pve01-h-full-validation-pass.log` (filename predates result; content records the failure).
- Next implementation: add an eval-only resolved-snapshot compatibility loader, keep normal `load_config` fail-closed, add a regression test, then rerun the identical H evaluation.

## 2026-07-18 — PVE-02 three-checkpoint command exceeded allocation (attempt 1)

- Command: sequential full-split validation of F, rebase-preserve, and replace-v48 on interactive job `2405414.opbs`, node `mg0006`.
- Expected: all three legacy observations complete under the frozen protocol.
- Actual: F completed successfully (`loss=3.0942679467`, 243 blocks, 248,589 predicted tokens). The allocation then reached its 30-minute walltime before rebase-preserve began model evaluation; PBS terminated the command and replace-v48 never started. Neither incomplete run has a result/summary attachment.
- Confirmed cause: allocation lifetime, not evaluator behavior. The empty rebase/replace log files and absent `validation_eval.json` prove no false success was written.
- Evidence: `artifacts/20260718-0945_pve02-f-full-validation-pass.log`; the same-prefix rebase/replace logs are zero bytes.
- Next attempt: obtain a fresh one-node allocation, recapture evaluator identity, then run the two missing checkpoints separately.

## 2026-07-18 — formal snapshot hygiene recheck

- The first local fingerprint check of `quality_20260718_1400` returned `sha256:0a3e98...` because the minimal snapshot copy omitted `.gitignore` while also copying untracked `__pycache__` files under the source scopes.
- Adding the repository `.gitignore` excluded generated bytecode and stabilized the source-only identity at `sha256:122f6982264d63cd06f39e06a35dcde81cde789d0713bf84a2ed2189d553b173`; an explicit post-import recapture returned the same value.
- Race audit found that the first baseline job `2405633` started during this correction and captured the old bytecode-contaminated fingerprint. It and dependent eval `2405634` are excluded from every formal pair.
- After re-running the mandatory PBS gate, replacement baseline seed-1337 train/eval jobs `2405771/2405772` were submitted. All other inspected quality jobs captured `sha256:122f698...`; the valid matrix uses the replacement.
