# plan04 FINAL evidence review

## Target and evidence policy

- Current source commit: `319081c8d57411a3e1b8ee724c82f810f25a3228`
- Current source fingerprint: `sha256:5ab9a68956dc718db713938957a0ab390cd00b0b5956e1740c6547154d772ebf`
- Result: **APPROVE**

The final evidence set intentionally contains three exact source identities. The user explicitly requires completed training results to remain valid across queue, wall-time, and harness-only changes; only training or protocol functional changes require reruns. `formal_manifest.json` records each gate's exact commit and fingerprint instead of claiming one shared fingerprint.

The diff from `7145197f3209fa67727bb0d458d0db38a81eb86d` to `a064ef8837ca033db883fe60879c4578b921b09c` changes scheduler queue/wall-time declarations and plan04 diagnostic-oracle behavior. The diff from `a064ef8837ca033db883fe60879c4578b921b09c` to `319081c8d57411a3e1b8ee724c82f810f25a3228` changes only the experiment supervisor, its tests, README, and plan acceptance wording. Neither interval changes `fs_diloco`, the model/data identity, optimizer workload, merge protocol, authority schema, or training implementation.

## Acceptance audit

The sole Full Protocol pass formula is:

`final Syncer PBS Exit_status = 0 AND terminal final_version = 25 AND final_mean_loss < 3.5`

All seven Full Protocol runs satisfy all three terms. Their final losses are `3.196270`, `3.197345`, `3.167654`, `3.288647`, `3.305553`, `3.243430`, and `3.245163`. Historical PBS records show exit status `0` for final Syncer jobs `2540864`, `2541161`, `2541236`, `2541649`, `2541825`, `2541896`, and `2541950`. Each terminal authority reports final version `25`.

The older Experiment 4 supervisor artifact reports a replacement-succession diagnostic failure. That result does not contradict the current pass formula: the same immutable run has final Syncer exit `0`, terminal version `25`, and final loss `3.288647`. The manifest records this run as an explicit user-policy adjudication without altering the old artifact.

## Requirement audit

- Baseline: both one-seed GPT-2/WikiText-2 runs completed. DDP loss/time is `2.935435`/`1127.79s`; periodic-average loss/time is `2.998504`/`1040.56s`.
- Workload: configs and all seven summaries record 200 local steps, 25 global synchronizations, eight contributors, and merge width four.
- Topology: every scenario submitted eight independent Learner jobs and one Syncer; Syncer failure and Dual Syncer contain a second Syncer epoch.
- Stagger behavior: 4+4 offsets are `0.07s` and `30.00s`; 3+3+2 offsets are `0.07s`, `32.02s`, and `60.00s`. No global version existed during the initial three-Learner phase.
- Learner faults: both scenarios deleted an admitted bootstrap Learner near 60 seconds and admitted one replacement.
- Syncer fault: primary `2541872` was deleted near 60 seconds; successor `2541896` was submitted about 30 seconds later and completed epoch 2.
- Dual Syncer: candidate `2541950` ran while authority still contained only the live primary epoch; after the primary deletion it acquired epoch 2 and completed.
- Summary and cleanup: all completed runs are present once in `runs/summary.csv`. Each Full Protocol run retains exactly one final model weight and one matching outer optimizer state.

## Verification

- Miyabi validation job `2541809.opbs`: Ruff format/check, focused pytest `271 passed`, full pytest `599 passed`, and website lint/test all passed on clean source.
- PBS/Bash syntax and every literal `group_list=xg24i002` directive passed static validation.
- Repository search found no old 40-minute, 2,000-step, 50-local-step, or 10-global-step plan04 entry.
- `qstat --limit` reports `0/16` running project jobs; `qstat` reports no unfinished job.
- Formal source scopes are clean. Unrelated user-owned report edits and deletions were excluded from plan04 commits.

No blocking finding remains. The implementation, test, configuration, launcher, current documentation, result manifest, and selected evidence are consistent with the latest plan04 acceptance policy.
