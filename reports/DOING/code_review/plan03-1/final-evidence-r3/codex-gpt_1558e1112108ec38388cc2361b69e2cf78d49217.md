# Codex final-evidence review

- Plan: `plan03-1`
- Review kind: `final-evidence`
- Formal source: `1558e1112108ec38388cc2361b69e2cf78d49217`
- Evidence target: `08050eed5da6e697437f796aabb7c980311c8937`
- Source fingerprint:
  `sha256:9fdff5a7c613e044a7f97523ede2c566f60d4fc9fbf5a5cb4edd5c416ee06337`
- Requirement: `DOCS-01`

## Evidence reviewed

I checked the fresh serial ladder against the formal contracts and the
documentation-complete source target. U1 job `2521752.opbs` binds one compute
node and the exact source identity; Ruff format/lint, 186 focused tests and 544
complete tests pass, and both JUnit projections are non-empty with zero
failure, error or skip. The artifact names its raw log and both XML files, and
all four files are tracked and hash-bound.

The normal, learner-replacement and syncer-takeover F1 artifacts bind the same
source, exact five-node topologies and 20 x 4 workload. Each has four accepted
updates per contributor and 5,120 direct applied tokens with zero balance. The
replacement evidence binds the sole old/new `learner_000` attempt lineage. The
takeover evidence binds committed version 2, a quiesced renewer, no active
SQLite transaction, primary exit 137 and the successor epoch. Their tracked
source-identity records are named by the gate artifacts.

Formal G1 job `2521829.opbs` binds nine distinct nodes, the canonical 50 x 10
static config and the same source identity. Versions are exactly `0..10`; each
of eight learners owns ten credits; 80 proposals and 64,000 direct tokens are
applied. The additional 23 proposals/18,400 tokens are durably dropped, giving
82,400 processed tokens with zero outstanding, conflicting, unpublished or
discarded fate and zero balance. Eight learner plus one syncer attestations,
the released epoch, terminal authority, 22 publication objects and SQLite
integrity all verify.

README and `docs/` name only current product/config/path/command surfaces. The
documented revision-11 51 x 11 run is correctly identified as over-baseline
DOCS-01 evidence and not as the final G1 gate. The final G1 result is retained
as formal acceptance evidence; the documentation does not overgeneralize its
asynchronous dropped-work count. No source-scope change occurred after the
common-target freeze.

The preformal schema-2 Codex artifact covers `SURFACE-01`, `CONFIG-01`,
`SCHEMA-01`, `CLEAN-01` and `ARCH-01`. The formal manifest registers one exact
artifact per gate, the complete U1 evidence set, named tracked support for each
runtime gate, and this final-evidence review. The requirement matrix binds all
non-final requirements to those current artifacts; `FINAL-01` remains pending
until staged aggregation.

No open Critical, High, Medium or Low finding remains. The packet is approved
for restored external multi-agent final-evidence review and subsequent staged
completion if no valid external finding is opened.

Verdict: APPROVE
