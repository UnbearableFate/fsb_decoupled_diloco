# Review request: plan03-1 R3 final common target

Review kind: `critical-incremental`

Plan ID: `plan03-1`

Continuity base: `47662f8d872f4a5e451908796a6b677105a28c52`

Frozen final common target: `1558e1112108ec38388cc2361b69e2cf78d49217`

Review the complete current target, concentrating on the final increment and
its consistency with the already-disposed FSD-R1 through R10 remediation. The
new source-scope changes synchronize documentation after a fresh authority
schema revision-11 nine-node run; the formal config must be restored to exactly
50 local steps × 10 global steps.

Priority checks:

- README/design/testing claims must match the tracked structured result and
  raw PBS evidence for job `2521638.opbs`, source
  `8dc2b727c64eab66f6627e1ed3a70ad9ef093631`, 9 nodes, 51 × 11 workload,
  versions `0..11`, 88 applied proposals/71,808 tokens, 22 dropped
  proposals/17,952 tokens and zero balance/outstanding fate;
- that over-baseline DOCS-01 result must not be represented as the final formal
  G1 gate, and the canonical static config must contain only 50 × 10;
- documentation of SQLite scan contention must exactly match the code's
  `BUSY`/`LOCKED` classifier and fail-fast integrity/I/O behavior;
- no code, schema, Checker, test, config, invocation, compatibility or
  persistence regression may hide among the report/evidence changes;
- the final target must remain one clean current-only design suitable for a
  fresh U1, three F1 scenarios and final 9-node G1 on one source fingerprint.

Inspect all directly affected source, config, docs, Checker and evidence. For
each finding, provide a stable ID, severity, exact file/line evidence, violated
invariant, concrete counterexample, smallest current-only fix and missing test
or durable oracle. Separate observed facts from inference. If there are no
findings, name the concrete modules, paths and identities inspected. Do not
recommend backward compatibility.

The final non-empty line must be exactly `Verdict: APPROVE` or
`Verdict: CHANGES_REQUIRED`.
