# Multi-agent review request

- Plan ID: `plan03-1`
- Review ID: `critical-current-fs-diloco`
- Review kind: `critical-incremental`
- Base commit: `7d4a607b753744d9b57b54fe0400d1267b13cc40`
- Target commit: `4ebee6339fb76f63127874c655d7b109b2ec0b39`
- PBS job ID: `2519854.opbs`
- Compute hostname: `mg0021`
- Target tree: `7135194e38cf6954bb6dc0c63d7fbfcb498ac847`
- Target diff SHA-256: `9385f8b44d72810717905e0f45edf56e3706dd7ce834bb573def5f5ce6cf8c95`
- Prompt SHA-256: `85dc084d65d501952af5429feb29be8ec5f41df849b3a8bb818a07dc7477668a`

## Common prompt

# External multi-agent review packet

Review the complete current `fs_diloco` implementation at the frozen target.
This is a read-only critical review, not a request to modify files or run an
expensive/full experiment.

## Identity

- Plan ID: `plan03-1`
- Review ID: `critical-current-fs-diloco`
- Review kind: `critical-incremental`
- Base commit: `7d4a607b753744d9b57b54fe0400d1267b13cc40`
- Target commit: `4ebee6339fb76f63127874c655d7b109b2ec0b39`
- Primary scope: all tracked files under `fs_diloco/` at the target
- Directly affected scope: `tests/`, `configs/`, `scripts/miyabi/`,
  `pyproject.toml`, `README.md`, and `docs/`

The branch-point diff is continuity context, but the requested review is of the
complete target-state package. Inspect directly affected callers, tests,
configuration, launchers, Checker, and documentation even where a file is not
prominent in the diff.

## Current design and requirements

The repository must expose exactly one current filesystem Decoupled DiLoCo
product: unversioned Full Protocol. There is no backward compatibility. Legacy
APIs, configs, data formats, schemas, paths, migration helpers, alternative
baseline, wrappers, aliases, fallbacks, deprecated behavior, and tests or docs
that exist only for them must be absent.

Key requirements are in:

- `plans/DOING/plans/plan03-1.md`
- `reports/DOING/plan03-1/requirement-matrix.csv`
- `reports/DOING/plan03-1/identity-authority.md`
- `reports/DOING/plan03-1/artifact-schema.md`
- `reports/DOING/plan03-1/p3-functional-fault-manifest.json`

Focus especially on:

1. identity and authority ownership across Git/config/run/PBS, contributor
   fences, leader epochs, immutable objects, SQLite, terminal state, and gate
   artifacts;
2. transaction, concurrency, crash/restart, replacement, takeover, replay,
   idempotency, flow-control, and terminal-drain invariants;
3. false-PASS or false-FAIL paths in tests and
   `scripts/miyabi/check_full_protocol_run.py`, including whether durable
   authority rather than process exit or logs proves success;
4. configuration uniqueness, dependency direction, public API boundaries,
   dead code, duplicate ownership, obsolete compatibility surfaces, unsafe
   cleanup, and documentation drift;
5. requirement-to-assertion coverage and missing adversarial or mutation tests.

## Frozen evidence available in the target

- One-node validation:
  `reports/DOING/plan03-1/artifacts/20260810-192000_p3-checker-remediation-validation.json`
  with matching raw log. It records clean source commit
  `44fe476898cd89e9b4fbb13930aa338e56cbd87f`, 119 focused tests and 505 full
  tests passing on PBS compute node `mg0018`.
- P3 normal/fault evidence and state are listed in
  `reports/DOING/plan03-1/workflow_state.yaml` and
  `reports/DOING/plan03-1/progress.md`.

The target commits after the validated source commit modify only tracked
review/evidence records. Do not treat a passing test count or PBS exit status as
sufficient proof. P3 fault scenarios, formal 8+1 validation, documentation
closure, and final evidence remain pending and must not be reported as complete.

## Required response

Return a complete Markdown review. For every finding include:

- stable finding ID and severity (`Critical`, `High`, `Medium`, or `Low`);
- exact file and line evidence plus the relevant control/data path;
- violated requirement or invariant and a concrete counterexample;
- required remediation and the smallest missing test or durable oracle.

Separate observed facts from inference. If no finding exists, list the concrete
modules, call paths, tests, and invariants inspected. Record requested and actual
model identity when it is verifiable. The final non-empty line must be exactly
`Verdict: APPROVE` or `Verdict: CHANGES_REQUIRED`.
