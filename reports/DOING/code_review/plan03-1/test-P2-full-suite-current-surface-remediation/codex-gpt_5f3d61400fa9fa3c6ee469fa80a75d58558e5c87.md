# Mandatory Codex incremental review

- Plan: `plan03-1`
- Review kind: `critical-incremental`
- Reviewed target: `5f3d61400fa9fa3c6ee469fa80a75d58558e5c87`
- Scope: remediation of full-suite attempt 1
- External review: skipped under the user's Codex-only review directive

## Scope inspected

I inspected all 16 failures, the current config dataclasses and validators,
strategy registry/classes, syncer startup admission path, tracked-surface oracle,
module-coverage manifest, both renamed protocol test modules and repository-wide
searches for removed spellings.

## Review result

The change is test-only and consistently follows the current design:

- The unversioned-surface oracle now uses `git ls-files` for exactly the set its
  name and invariant claim. Ignored runtime bytecode cannot create false
  failures. The two tracked test module paths were renamed from `_v1`/`_v2` and
  the only manifest references changed atomically. Artifact format-version class
  names inside those tests remain legitimate current schema identities, not a
  Full Protocol generation surface.
- The startup fixture now matches the sole current `Config` layout: membership
  and sync sections live directly under `loaded.config`. No `shared` wrapper or
  fallback was added to production.
- Every adoption test supplies the exact names owned by current strategy
  classes and config validation. The helper enables the required inner polling
  flag for non-replace strategies while retaining current outer-Nesterov,
  zero-weight-decay and adopt-after-upload defaults. Removed aliases no longer
  occur as string values anywhere in active source/tests/config/docs/plans.
- No production source, API, persisted format, launcher or Checker changed.
  The 278 ignored bytecode files found under repository test/package roots were
  deleted as generated cache, including legacy module names; no tracked file was
  removed by that cleanup.

## Findings

No Critical, High, Medium or Low finding remains. Full-suite attempt 2 may run
in the retained one-node allocation. A failure must be recorded before any
additional change.

Verdict: APPROVE
