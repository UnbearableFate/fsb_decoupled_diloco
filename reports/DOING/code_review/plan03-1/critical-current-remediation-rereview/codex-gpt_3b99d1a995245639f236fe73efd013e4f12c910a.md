# Codex external-finding remediation rereview

- Plan: `plan03-1`
- Review kind: `critical-incremental`
- Rejected target: `74ecd4fb64311c69ae0d758d8c1d99b27a9c5572`
- Tested target: `3b99d1a995245639f236fe73efd013e4f12c910a`
- Source fingerprint:
  `sha256:e3edbda02aec61c90dcf3e7b8e88c0becee05c7dd2bc40b9671f9b77f229a367`
- External results for this review round: not read; the rereview job has not
  been submitted

## Review performed

I reviewed the complete increment from the rejected external-review target,
then rechecked each affected current caller and acceptance boundary rather than
reviewing only isolated hunks. The review covered syncer receipt/proposal
ingestion, SQLite result-code handling, static replacement authorization,
candidate failure/release ordering, runtime entrypoints, cleanup path ownership,
the Full Protocol launcher and Checker, the final completion contract, artifact
schema, architecture inventories and their positive and negative tests.

The prior valid Claude report and Codex dispositions were treated as mandatory
inputs. The invalid GLM and DeepSeek transcripts were not treated as approval;
the valid MiniMax approval did not override Claude's findings.

## Finding closure

- FSD-R1 and R6 are closed. The launcher owns boundary value `2`, exports it to
  rank execution and the Checker, the runtime artifact publishes the received
  value, and the completion contract pins it only for the takeover gate. A
  Checker fixture proves boundary `3` is accepted only when its durable fault
  record also says `3`; a separate completion mutation proves a `3` artifact is
  rejected against the registered value `2`.
- FSD-R2 is closed without restoring a catch-all. Only SQLite `BUSY` and
  `LOCKED` result-code families are classified as retryable scan contention.
  `IOERR` and every unclassified operational error propagate.
- FSD-R3 and R4 are closed. Real `LeaderAuthority` composition proves exact
  static replacement authorization and rejects an old-fence mismatch. Both
  receipt and proposal ingest branches propagate schema, command-conflict and
  stale-token failures.
- FSD-R5 is closed by making cleanup consume `RunPaths.epoch_stop_path`; its
  test fixture now consumes the same owner instead of duplicating hashing and
  formatting.
- FSD-R7 is closed. Candidate failure state is set before error publication or
  error fencing can fail, so the release path cannot replace the primary
  exception or mislabel a failed candidate. The success/release-failure and
  body/fencing-double-failure paths are both tested.
- FSD-R8 and R9 are closed by deleting the duplicate runtime-module `__main__`
  surfaces and making the storage dependency inventory explicitly non-empty.

During verification I found five test-contract defects rather than product
defects: the expanded startup fixture lacked the current bootstrap-marker path;
two direct Checker invocations omitted the new required boundary argument; a
self-proof artifact omitted the new field; the Hub revision test still relied
on the former Hub-model default; and completion lacked a direct mismatched
boundary mutation. All five are fixed in the tested target. No compatibility
path or second interface was added.

## Validation evidence

Interactive PBS job `2521364.opbs` ran on exact one-node topology `mg0008` from
the tested clean source. The registered producer passed Ruff format and lint,
186 focused tests and all 544 repository tests with zero failures, errors or
skips. Its artifact is
`reports/DOING/plan03-1/artifacts/u1_validation_3b99d1a.json`; the artifact,
raw log, focused JUnit and full JUnit SHA-256 values are respectively
`76b774d65e54db957616ada5b13ed580d1d649f30b7590942695b827d6986d8b`,
`985ca287578524399c9670c766d603ea6a96bef919c7f5ee4f8df8fb6cd42266`,
`51ac59bb5748310cad9ce378cca65618c37c87d473a2f8d148602b24e8684cea`
and `89c63d59a835c68a7b426f9f028e71ad2dc29591c63ecb1a10e22e71589983ba`.

No open Critical, High, Medium or Low finding remains in the tested current
source. It is approved for the restored external multi-agent rereview. Any
valid external finding remains blocking until independently checked and
disposed.

Verdict: APPROVE
