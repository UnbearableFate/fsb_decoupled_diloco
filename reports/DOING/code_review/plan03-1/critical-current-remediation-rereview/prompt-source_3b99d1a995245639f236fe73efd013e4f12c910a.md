# Review request: plan03-1 external-finding remediation rereview

Review kind: `critical-incremental`

Plan ID: `plan03-1`

Rejected target: `74ecd4fb64311c69ae0d758d8c1d99b27a9c5572`

Frozen tested target: `3b99d1a995245639f236fe73efd013e4f12c910a`

Review the complete current source and the remediation increment from the
rejected target. The prior external round produced valid Claude findings
FSD-R1 through FSD-R9, recorded in
`reports/DOING/code_review/plan03-1/critical-current-remediation/finding-dispositions_74ecd4fb64311c69ae0d758d8c1d99b27a9c5572.md`.
Verify that the target closes every accepted finding without introducing a new
current-design defect. Do not assume a disposition is correct merely because
it says accepted or closed.

Priority invariants:

- the takeover boundary has one launcher owner, is passed unchanged to rank
  execution and the Checker, is durable in the gate artifact and is pinned by
  the final takeover completion contract;
- only SQLite `BUSY`/`LOCKED` contention is retried by receipt/proposal scans;
  I/O, schema, command-conflict and stale-token failures propagate;
- static replacement requires and consumes the exact current-fence
  authorization through real authority composition;
- a failed candidate never executes the success release path even when error
  publication or fencing also fails, and its primary exception remains primary;
- cleanup uses the canonical `RunPaths` owner; runtime actors have one public
  invocation boundary; repository dependency/content inventories cannot pass
  vacuously;
- positive tests execute the intended behavior and negative tests mutate the
  same acceptance boundary, including a mismatched takeover value at both the
  run Checker and final completion layers.

The target passed Ruff format/lint, 186 focused tests and 544 complete tests on
one Miyabi compute node with zero failures, errors or skips. Treat that as
supporting execution context, not as a substitute for source review.

Inspect all directly affected implementation, scripts, Checkers, tests and
current documentation. For each finding, provide a stable ID, severity, exact
file/line evidence, the violated invariant, a concrete counterexample, the
smallest current-only fix and the missing test or durable oracle. Separate
observed facts from inference. If there are no findings, name the concrete
modules, paths and invariants inspected. Do not recommend backward
compatibility.

The final non-empty line must be exactly `Verdict: APPROVE` or
`Verdict: CHANGES_REQUIRED`.
