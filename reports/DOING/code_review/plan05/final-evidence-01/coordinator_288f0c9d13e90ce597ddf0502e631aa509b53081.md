# plan05 FINAL evidence review

## 结论

`APPROVE`。12 项 requirement 均由同一 `FINAL_COMMON_TARGET` 的有效 evidence 支持；正式 config、workload、scheduler topology、durable authority oracle、文档和当前实现一致。没有 open blocking finding、wrong-source artifact、自证循环或仍在 active/queued 的 plan05 PBS job。正式 run root、authority、checkpoint 和原始日志继续保留，没有执行 material cleanup。

## Target identity

- Commit：`288f0c9d13e90ce597ddf0502e631aa509b53081`
- Tree：`49b0a0bcb5568675a1f0b7259392e748ede7d3c5`
- Dirty：`false`
- Source fingerprint：`sha256:d2e3b725d16de5c8d9768386cfabd42f94dbaf865156bb334e2c67ccf6fed3e4`
- Source scopes：`fs_diloco`、`configs`、`do_experiments`、`scripts/miyabi`、`tests`、`tools`、`torch_ddp_baselines`、`pyproject.toml`、`README.md`、`docs`、`plans/00-RESEARCH_PLAN.md`、`website/app`、`website/scripts`。

U1、两个 functional artifact 和三个 formal artifact 的 commit、clean flag、fingerprint 与 scopes 均精确匹配。旧 target 的 PASS/FAIL artifact 只用于失败事实，不进入最终证据集。

## Evidence review

| 证据 | 独立检查 | 结论 |
|---|---|---|
| `validation_candidate30.json` | PBS `2533273.opbs`；Ruff format/lint；focused 264/264；full 595/595；website lint/test；errors 为空 | PASS |
| `functional_no_failure_final_288f0c9.json` | PBS `2533382.opbs`；4 learner + 1 syncer；v4；12 applied/4 dropped proposal；ledger balance 0；10 个 publication object identity | PASS |
| `functional_syncer_takeover_final_288f0c9.json` | PBS `2533383.opbs`；primary epoch terminal 后 successor 接管；v4；无 stale commit；receipt/proposal 与 ledger exact | PASS |
| `20260812_050805_no_failure.json` | PBS `2533369.opbs`；8 个独立 learner + 1 个独立 syncer，9 个不同 host；10 个 exact four-way merge；40 applied update；8 个 ack fence；ledger balance 0 | PASS |
| `20260812_045342_failure_no_replacement.json` | PBS `2533281.opbs`；一个 admitted victim 被 exact `qdel`；0 launch request；stream 2 pre-receipt hard crash；gap upper bound `3276800`；无 receipt/progress；ledger balance 0 | PASS |
| `20260812_050107_failure_authorized_replacement.json` | PBS `2533326.opbs`；一个 capacity observation→launch request→qsub receipt→admission chain；stream 4 epoch 1→2；旧 fence 无 late receipt/update；ledger balance 0 | PASS |

三个正式场景均使用 GPT-2 revision `607a30d783dfa663caf39e06633721c8d4cfcd7e`、WikiText-2 revision `b08601e04326c79dfdd32d625aee71d232d685c3`、BF16、seed `1337`、block size 1024、micro batch 2、gradient accumulation 8、200 inner step 和 10 个 global version。fixed 与 scaling config 只按预注册场景改变 capacity policy；membership、fence、admission、model/data 和训练 contract 不变。

每份 formal artifact 都包含 archive-aware logical history、scheduler history、actor attestation、receipt/cursor chain、token fate、terminal fence 和 v0 至 v10 的 22 个 checkpoint publication identity。存在的 object 必须是 canonical immutable regular file，并匹配 size/SHA-256；缺失 object 只在 immutable archive 已验证、hot row 不存在且没有 pending/claimed GC candidate 时接受。Mutation tests 覆盖 retained-object drift、未完成 GC、非 crash 缺 progress、无 progress 但存在 update，以及 nullable source-lock identity。

## Requirement closure

- P05-R01 至 P05-R07：strict parser/schema/protocol/authority/runtime/tool/docs tests 与最终 dead-surface search 证明唯一 stream/instance admission 设计，旧字段、旧 schema、旧 API、旧配置和兼容路径已删除。
- P05-R08：同一 target 的 U1 与两个 functional harness 全部通过。
- P05-R09：coordinator PREFORMAL finding 全部关闭；外部 runner 始终只请求 `opencode-go/deepseek-v4-flash`。old-target 完整 review 和 final-target 增量 review 均 timeout 且 raw 为 0；三个中间增量 review 因 formal failure 使 target 失效而终止。所有轮次均无外部 finding，均未被误记为 `APPROVE`。
- P05-R10：三组 fresh-root formal scenario 均 PASS。Plan04 没有 latest-target、相同 input/workload/seed/terminal contract 的完成 Dynamic Full baseline，因此比较严格记为 `incomparable`，不执行 20% threshold，也不使用 DDP、Periodic Average 或 diagnostic substitute。
- P05-R11：no-failure scheduler evidence 精确证明 8 个 learner job 和 1 个 syncer job 分布于 9 个不同 compute host，并完成 200 × 10 contract。
- P05-R12：formal manifest、requirement matrix、最终报告和本审查形成完整引用闭包；archive map 与独立 move-only commit 是审查后的唯一剩余动作。

## Cleanup and job ownership

三个 formal artifact 的 `cleanup.active_or_queued_after_check` 都为空，且各自列出 exact owned actor job。最终检查 `2533273`、`2533281`、`2533326`、`2533369`、`2533382`、`2533383` 和外部 review `2533421` 均返回 `No unfinished job found`；user-visible `qstat -a` 中没有 plan05 owned row。

没有删除 run root、authority database、checkpoint、archive、formal log 或 reviewer evidence。允许执行的归档仅移动 plan/report/review 路径，并保留 path mapping 与 Git history。

FINAL verdict: APPROVE
