# S2 implementation progress

## 2026-07-17 19:15 JST — S2-L0–L3 / REC-01–REC-06

- Baseline source: `633b4f55e3be498804899094e9b709b6fa61ca6a`; the implementation worktree was dirty only with the plan corrections and work described here. Baseline execution used a detached worktree at that exact commit.
- Plan audit corrections: the original order required S4 to produce a trace comparison before S2 delivered the comparator; S2 is now first. Exact whole-run trace equality was also unsound for asynchronous full/fragment syncer logs: unchanged repeat runs differed in liveness/quorum ordering and, for full mode, normal-grace versus terminal-drain selection timing. The plans now require repeatability proof for any gated projection. The original smoke launcher also always started two learners even for the one-learner prediction config; it now accepts `NUM_LEARNERS` and passes the matching CLI override to every process.
- Implementation: added a profile-driven per-actor JSONL comparator with exit 0/1/2 semantics; added explicit frozen `PredictionState` and a single `reconcile_prediction` implementation; rewired both prediction reconcile call sites through it without changing event payload order or fields.
- Baseline repeatability: two independent 1-learner runs of `configs/fs_diloco_tiny_predict_local.yaml` on baseline commit compared equivalent with `learner-adoption-v1`. Evidence: `artifacts/20260717-baseline_repeatability.txt`; raw runs: `/work/xg24i002/x10041/fsb_decoupled_diloco/reports/imp_plans/bug_fixing/artifacts/baseline_runs/predict_1a/run` and `predict_1b/run`.
- RED evidence: collection failed on the intentionally missing comparator and prediction-state symbols before implementation: `artifacts/20260717-s2-red_fail.log`.
- Targeted verification: `.venv/bin/pytest -q tests/test_compare_event_traces.py tests/test_learner_rebase.py` → 27 passed (`artifacts/20260717-targeted_pytest.log`). This covers comparator equality/difference/invalid-input cases (REC-01), state clearing and event-field delta (REC-02/03), timeout without partial reconciliation (REC-05), and abandon-on-stop (REC-06).
- Trace equivalence: the baseline 1-learner prediction run and current implementation run compared equivalent with `learner-adoption-v1` (REC-04): `artifacts/20260717-rec04_trace_1learner.txt`. Current raw run: `artifacts/predict_current/run`.
- Full regression: `.venv/bin/pytest -q` → 128 passed in 39.55s (`artifacts/20260717-full_pytest_final.log`). Static checks: `.venv/bin/ruff check fs_diloco tests`, `git diff --check`, and `bash -n scripts/local/run_tiny_2proc_smoke.sh` all passed.
- Runtime environment: confirmed PBS allocation `2403932.opbs`, one Miyabi-G compute node `mg0007`, default modules `nvidia/25.9` and `nv-hpcx/25.9`, Python 3.13.13, PyTorch 2.13.0+cu132, CUDA available.
- Remaining risk: the comparator can only prove the fields selected by its named profile; S3 must continue to pair projected traces with controlled tests and terminal invariants because asynchronous scheduling is intentionally not canonicalized.
